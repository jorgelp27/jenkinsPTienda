version: '3'
services:
  web:
    build:
      context: ${DIR_DFLIES}
      dockerfile: ./Dokerfile
    env_file:
      -./.env
    ports:
      - ${PUERTO}:80
    # volumenes:
    #   - ../app_next/usr/share/nginx/html
    